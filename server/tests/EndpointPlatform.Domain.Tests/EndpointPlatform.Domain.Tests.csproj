<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>EndpointPlatform.Domain.Tests</RootNamespace>
    <AssemblyName>EndpointPlatform.Domain.Tests</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Domain\EndpointPlatform.Domain.csproj" />
    <!--
      Test-only, and only for the recovery-password conformance test. The agent
      and the server state the same validation rule in two places because the
      agent cannot reference the server's domain; this reference is what lets a
      test drive both over one corpus and prove they still agree. No production
      assembly gains a reference, and ArchitectureTests inspects production
      assemblies only.
    -->
    <ProjectReference Include="..\..\..\agent\EndpointAgent.Core\EndpointAgent.Core.csproj" />
    <!--
      Test-only, for the Chrome contract-alignment tests. The domain's
      ChromeReportStatus and ChromeExtensionInstallType enums are mapped from the
      wire contract's status and install-type strings by name, and the domain
      cannot reference Contracts to share the sets; this reference is what lets a
      test prove the two still agree. Same reasoning as the agent reference above.
    -->
    <ProjectReference Include="..\..\..\shared\Contracts\EndpointPlatform.Contracts.csproj" />
  </ItemGroup>

</Project>
